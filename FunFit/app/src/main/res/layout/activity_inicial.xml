<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    tools:context="salt.movil.funfit.ui.InicialActivity">

    <EditText
        android:textColorHint="@color/colorAccent"
        android:id="@+id/edit_username_inicial_activity"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:hint="username"
        android:layout_width="200dp"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/btn_continue_inicial_activity"
        android:background="@color/colorPrimary"
        android:text="continue"
        android:textColor="@color/colorAccent"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/edit_username_inicial_activity"
        android:layout_width="200dp"
        android:layout_height="wrap_content" />

    <TextView
        android:visibility="gone"
        android:id="@+id/txt_registered_user_initial_activity"
        android:layout_marginBottom="20dp"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/edit_username_inicial_activity"
        android:text="Registrado, espera a que inicie la patida..."
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
